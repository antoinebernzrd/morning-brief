# macOS
.DS_Store

# Telegram — credentials and session must NEVER be committed
telegram_config.json
telegram_session.session
telegram_session

# API keys
openai_config.json

# Generated output (published via GitHub Actions → GitHub Pages)
index.html

# Runtime cache — grows indefinitely, not source code
headline_cache.json
dashboard.log

# Scratch / temp files
news_fetcher trash.py
*.pyc
__pycache__/
.claude/
